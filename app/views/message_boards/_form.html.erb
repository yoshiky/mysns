<%= form_for(@message_board) do |f| %>
  <% if @message_board.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@message_board.errors.count, "error") %> prohibited this message from being saved. </h2>
      <ul>
	<% @message_board.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
	<% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %>
    <%= f.text_field :title %>
  </div>

  <div class="field">
    <%= f.label :body %>
    <%= f.text_area :body %>
  </div>

  <div class="action">
    <%= f.submit %>
  </div>
<% end %>
